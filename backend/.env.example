# Copy to .env on the server — never commit real keys
API_FOOTBALL_KEY=

# Optional: blend predictions with bookmaker odds (the-odds-api.com)
THE_ODDS_API_KEY=

# Optional: run `python run_fetch_nt_history.py` to pull qualifiers + friendlies (2018–2026)
