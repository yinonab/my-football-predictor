# Render.com — https://render.com/docs/deploy-fastapi
services:
  - type: web
    name: football-predictor-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python run_build_ratings.py
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: API_FOOTBALL_KEY
        sync: false
      - key: THE_ODDS_API_KEY
        sync: false
      - key: GITHUB_GIST_TOKEN
        sync: false
      - key: GITHUB_GIST_ID
        sync: false

  - type: web
    name: football-predictor-web
    runtime: static
    plan: free
    staticPublishPath: mobile/build/web
    buildCommand: echo "Flutter web pre-built in repo"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
